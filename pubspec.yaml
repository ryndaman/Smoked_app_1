name: smoked_1
description: "A new Flutter project."
publish_to: 'none'

version: 0.9.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.3
  fl_chart: ^1.0.0
  intl: ^0.20.2
  font_awesome_flutter: ^10.7.0
  csv: ^6.0.0
  path_provider: ^2.1.3
  share_plus: ^11.0.0
  provider: ^6.1.2  # ADDED: Provider for state management

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/

